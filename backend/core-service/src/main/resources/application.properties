# Quarkus Core Service Configuration
quarkus.application.name=core-service
quarkus.application.version=1.0.0

# Server Configuration
quarkus.http.port=8080
quarkus.http.host=0.0.0.0

# Logging Configuration
quarkus.log.level=INFO
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
quarkus.log.console.enable=true
quarkus.log.file.enable=false

# Prometheus Metrics
quarkus.micrometer.export.prometheus.enabled=true
quarkus.micrometer.enabled=true

# Health Checks
quarkus.smallrye-health.path=/health

# OpenAPI/Swagger
quarkus.smallrye-openapi.path=/swagger-ui
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.url=/openapi

# Datasource (using H2 for development, can be switched to PostgreSQL)
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:coredb
quarkus.datasource.username=sa
quarkus.datasource.password=

# Hibernate Configuration
quarkus.hibernate-orm.database.generation=create-drop
quarkus.hibernate-orm.log.sql=false
