apiVersion: v1
kind: ConfigMap
metadata:
  name: application-config
  namespace: default
data:
  # Application-specific configurations
  app-name: "my-application"
  log-level: "info"
  database-url: "mysql://user:password@localhost:3306/db"
  feature-toggle: "true"
